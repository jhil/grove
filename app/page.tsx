import { HomeContent } from "@/components/home/home-content";

/**
 * Landing page for Plangrove.
 * Shows onboarding for new users, regular content for returning users.
 */
export default function HomePage() {
  return <HomeContent />;
}
